import React from "react";

const PropertyConfiguration = ({ toolItem }) => {
  return (
    <div className="configuration-box">
      <div>{toolItem.name}</div>
    </div>
  );
};

export default PropertyConfiguration;
